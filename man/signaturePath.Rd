% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signaturePath.R
\name{signaturePath}
\alias{signaturePath}
\title{Path-valued truncated signature}
\usage{
signaturePath(X, depth, includeLevelZero = TRUE, sep = ",")
}
\arguments{
\item{X}{a numeric matrix (T x d) or vector (treated as 1D).}

\item{depth}{non-negative integer truncation level.}

\item{includeLevelZero}{if \code{TRUE} (default), keep the first column
(empty-word, always 1). If \code{FALSE}, drop it.}

\item{sep}{separator for word names (default \code{","}).}
}
\value{
a numeric matrix of shape \verb{T x p} (or \verb{T x (p - 1)}). Column
names are word strings; row names are \code{NULL}.
}
\description{
Returns the signature of every prefix of the path. The result is a
\verb{T x p} matrix whose row \code{t} is the signature of \code{X[1:t, , drop = FALSE]};
row 1 is the identity \verb{(1, 0, 0, ...)} and row \code{T} matches
\code{signature(X, depth)}.
}
\details{
Useful for rolling / window applications, plotting signature
trajectories, and computing partial signatures on any sub-interval as
\code{result[end, ]} "minus" \code{result[start, ]} (via tensor-algebra inverse,
deferred to v0.4's \code{chenProduct()} / \code{tensorInverse()}).
}
\examples{
X <- matrix(cumsum(rnorm(60)), 30, 2)
sp <- signaturePath(X, depth = 2)
sp[1, ]              # identity (1, 0, 0, 0, 0, 0, 0)
all.equal(sp[30, ], signature(X, depth = 2))

}
\seealso{
\code{\link[=signature]{signature()}}, \code{\link[=signatureBatch]{signatureBatch()}}
}
