% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enumerateWords.R
\name{enumerateWords}
\alias{enumerateWords}
\title{Enumerate all multi-indices over an alphabet of size d}
\usage{
enumerateWords(dim, depth)
}
\arguments{
\item{dim}{alphabet size (number of path channels), a positive integer}

\item{depth}{truncation level, a non-negative integer}
}
\value{
A list of integer vectors of length \code{sum(dim^(0:depth))}. The
empty word is \code{integer(0)}.
}
\description{
Generates the list of words used to index the truncated signature, in the
canonical level-major, lexicographic-within-level ordering: empty word
first (position 1), then all level-1 words (positions 2 to d+1), then
all level-2 words, and so on. Within each level the last letter varies
fastest.
}
\details{
For a level-k word at position \code{w >= 2}, its parent (level k-1) is at
position \code{floor((w-2)/d) + 1} and its last letter is \code{((w-2) \%\% d) + 1}.
This identity is what the signature kernel uses to build level-k columns
from level-(k-1) columns.
}
\examples{
enumerateWords(2, 2)
#> [[1]] integer(0)
#> [[2]] 1
#> [[3]] 2
#> [[4]] 1 1
#> [[5]] 1 2
#> [[6]] 2 1
#> [[7]] 2 2

}
