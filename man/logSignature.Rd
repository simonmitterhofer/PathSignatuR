% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logSignature.R
\name{logSignature}
\alias{logSignature}
\title{Log-signature of a discrete path}
\usage{
logSignature(X, depth, includeLevelZero = TRUE, sep = ",")
}
\arguments{
\item{X}{a numeric matrix (T x d) or vector (treated as 1D path).}

\item{depth}{non-negative integer truncation level.}

\item{includeLevelZero}{if \code{TRUE} (default), the returned vector
starts with the empty-word entry (always \code{0}, since \verb{log 1 = 0}).
If \code{FALSE}, that entry is dropped.}

\item{sep}{separator for word names (default \code{","}).}
}
\value{
a named numeric vector of length \code{sum(d^(0:depth))} (or
\code{... - 1} when \code{includeLevelZero = FALSE}).
}
\description{
Computes the formal logarithm of the truncated signature in the
truncated tensor algebra:
\verb{logSig(X) = sum_\{k = 1\}^\{N\} (-1)^\{k+1\} (S - e)^\{(x) k\} / k},
where \code{S = signature(X, depth)} and \verb{e = (1, 0, 0, ...)}. The series
is finite because \code{(S - e)} has zero level-0 coefficient, so
\verb{(S - e)^\{(x) k\}} has minimum level \code{k} and contributes nothing
above level \code{N}.
}
\section{Representation}{

The log-signature lies in the free Lie algebra of rank \code{d}, which
is a strict subspace of the truncated tensor algebra. The returned
vector represents the log-signature as a general tensor algebra
element in canonical word order; entries are therefore not
independent (they satisfy linear relations induced by the Jacobi
identity and antisymmetry of the Lie bracket). Projection onto a
Hall or Lyndon basis is left for a future release.
}

\section{Linear paths}{

For a linear path from \code{a} to \code{b}, the signature is the tensor
exponential of the increment \code{delta = b - a}, so the log-signature
is exactly \verb{(0, delta_1, ..., delta_d, 0, 0, ...)} -- level-1
coordinates carry the increment, all higher levels are zero.
}

\examples{
x <- cumsum(rnorm(50))
logSignature(x, depth = 4)   # 1D: only level-1 entry is nonzero

X <- matrix(cumsum(rnorm(60)), 30, 2)
logSignature(X, depth = 3)

}
\seealso{
\code{\link[=signature]{signature()}}, \code{\link[=tensorProduct]{tensorProduct()}}, \code{\link[=tensorInverse]{tensorInverse()}}
}
