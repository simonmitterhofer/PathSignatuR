% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signatureBatch.R
\name{signatureBatch}
\alias{signatureBatch}
\title{Truncated path signatures of a batch of paths}
\usage{
signatureBatch(X, depth, includeLevelZero = TRUE, sep = ",")
}
\arguments{
\item{X}{one of:
\itemize{
\item a list of \verb{T_i x d} numeric matrices (variable \code{T_i} allowed; all
must share the same \code{d}),
\item a 3D numeric array of shape \verb{T x d x nPaths} (uniform \code{T}),
\item a single matrix or vector (treated as a 1-path batch).
}}

\item{depth}{non-negative integer truncation level.}

\item{includeLevelZero}{if \code{TRUE} (default), the first column is the
empty-word entry. If \code{FALSE}, that column is dropped.}

\item{sep}{separator for word names (default \code{","}).}
}
\value{
a numeric matrix of shape \verb{nPaths x p}, where \code{p = sum(d^(0:depth))}
(or \code{p - 1} when \code{includeLevelZero = FALSE}). Row \code{i} is the terminal
signature of the \code{i}-th path. Column names are word strings. Row
names come from \code{names(X)} for a list input or \code{dimnames(X)[[3]]}
for a 3D array.
}
\description{
Computes terminal signatures for many paths in one call, sharing the
\verb{(d, depth)} precompute across all paths. Equivalent to applying
\code{\link[=signature]{signature()}} to each path individually but considerably faster for
large batches of short paths.
}
\examples{
paths <- list(
  matrix(cumsum(rnorm(40)), 20, 2),
  matrix(cumsum(rnorm(60)), 30, 2),
  matrix(cumsum(rnorm(50)), 25, 2)
)
signatureBatch(paths, depth = 2)

}
\seealso{
\code{\link[=signature]{signature()}}, \code{\link[=signaturePath]{signaturePath()}}
}
