% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leadLag.R
\name{leadLag}
\alias{leadLag}
\title{Lead-lag transform of a path}
\usage{
leadLag(X)
}
\arguments{
\item{X}{numeric matrix (T x d) or vector (treated as 1D path).}
}
\value{
a numeric matrix of shape \verb{(2T - 1) x (2d)}. Columns are
interleaved \verb{lead_1, lag_1, lead_2, lag_2, ...} and named
accordingly. Row names are stripped.
}
\description{
Applies the Flint-Hambly-Lyons lead-lag transform: each channel of
\code{X} is duplicated into a "lead" and a "lag" copy that advance in
alternation, producing a path of length \verb{2T - 1} and dimension \verb{2d}.
}
\details{
The point of the transform is that the level-2 cross-signature term
\verb{S^\{lead_i, lag_i\}} of the transformed path recovers the quadratic
variation of channel \code{i} of the original path (up to a factor of
1/2). This lets the signature pick up volatility-like information
that a piecewise-linear path would otherwise miss.

For a 1D path \verb{(x_1, x_2, x_3)} the output is

\if{html}{\out{<div class="sourceCode">}}\preformatted{        lead_1   lag_1
   t=1:  x_1     x_1
   t=2:  x_2     x_1     (lead jumped)
   t=3:  x_2     x_2     (lag caught up)
   t=4:  x_3     x_2     (lead jumped)
   t=5:  x_3     x_3     (lag caught up)
}\if{html}{\out{</div>}}
}
\section{Quadratic variation}{

For \code{X} a 1D path and \code{LL = leadLag(X)}, the level-2 cross term
\verb{S^\{lead, lag\}(LL) - S^\{lag, lead\}(LL)} equals the signed area
enclosed by the lead-lag staircase, which in turn equals (up to
sign and a factor) the quadratic variation of \code{X}. See Flint,
Hambly & Lyons (2016) for the precise statement.
}

\section{Sizing}{

Lead-lag doubles the channel count, which inflates the truncated
signature size from \code{sum(d^(0:N))} to \verb{sum((2d)^(0:N))}. For
\verb{(d, N) = (3, 4)} that's 121 vs 1365 terms. Plan depth accordingly.
}

\examples{
# 1D path: classic staircase
leadLag(c(1, 2, 3))
#>      lead_1 lag_1
#> [1,]    1     1
#> [2,]    2     1
#> [3,]    2     2
#> [4,]    3     2
#> [5,]    3     3

# 2D path: columns are lead_1, lag_1, lead_2, lag_2
X <- matrix(rnorm(20), 10, 2)
LL <- leadLag(X)
dim(LL)
#> [1] 19  4
colnames(LL)
#> "lead_1" "lag_1" "lead_2" "lag_2"

}
\seealso{
\code{\link[=signature]{signature()}}, \code{\link[=basepointAugment]{basepointAugment()}}, \code{\link[=timeAugment]{timeAugment()}}
}
