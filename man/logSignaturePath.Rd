% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logSignaturePath.R
\name{logSignaturePath}
\alias{logSignaturePath}
\title{Path-valued log-signature}
\usage{
logSignaturePath(X, depth, includeLevelZero = TRUE, sep = ",")
}
\arguments{
\item{X}{a numeric matrix (T x d) or vector (treated as 1D path).}

\item{depth}{non-negative integer truncation level.}

\item{includeLevelZero}{if \code{TRUE} (default), keep the first column
(empty word, always 0). If \code{FALSE}, drop it.}

\item{sep}{separator for word names (default \code{","}).}
}
\value{
a numeric matrix of shape \verb{T x p} (or \verb{T x (p - 1)}). Column
names are word strings; row names are \code{NULL}.
}
\description{
Returns the log-signature at every prefix of the path. The result is
a \verb{T x p} matrix whose row \code{t} is \code{logSignature(X[1:t, , drop = FALSE], depth)};
row 1 is zero (log of the identity), and row \code{T} matches
\code{logSignature(X, depth)}.
}
\section{Implementation}{

One-pass C++ kernel: maintains the running signature \code{S} and, after
each Chen update, expands \code{log(S)} via the truncated Neumann series
\verb{sum_\{k=1\}^N (-1)^\{k+1\} (S - e)^\{(x) k\} / k}. Uses the same cached
\verb{(d, depth)} workspace as the signature kernel.
}

\examples{
X  <- matrix(cumsum(rnorm(60)), 30, 2)
lp <- logSignaturePath(X, depth = 2)
lp[1, ]                                    # zero row (log of identity)
all.equal(lp[30, ], logSignature(X, depth = 2))

}
\seealso{
\code{\link[=logSignature]{logSignature()}}, \code{\link[=signaturePath]{signaturePath()}}
}
