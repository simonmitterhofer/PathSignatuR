% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shuffleProduct.R
\name{shuffleProduct}
\alias{shuffleProduct}
\title{Truncated shuffle product}
\usage{
shuffleProduct(a, b, depth)
}
\arguments{
\item{a, b}{numeric vectors of equal length matching \code{sum(d^(0:depth))}
for some positive integer \code{d}. Must be finite.}

\item{depth}{non-negative integer truncation level.}
}
\value{
a named numeric vector of the same length as \code{a} and \code{b}.
}
\description{
Computes the shuffle product of two elements \code{a} and \code{b} of the
truncated tensor algebra. For each output word \code{w} of length \code{k},
\verb{(a ⧢ b)[w] = sum over subsets S of \{1..k\} of a[w|S] * b[w|S^c]}.
Equivalently, sums over all \code{2^k} interleavings of subwords.
}
\details{
The shuffle product is \strong{commutative and associative}, unlike the
tensor product. The identity tensor \verb{(1, 0, 0, ...)} is the unit
for both products.
}
\section{Shuffle identity for signatures}{

For any path \code{X} with truncated signature \code{S}, and any two words
\code{u}, \code{v} with \verb{|u| + |v| <= depth},
\verb{<S, u> * <S, v> = <S, u ⧢ v>} (scalar multiplication on the left
equals shuffle on the right). This means coordinates of a signature
aren't independent: products of low-level coordinates are linear
combinations of high-level coordinates.
}

\examples{
a <- c(1, 0.5, -0.2, 0.1, 0, 0.3, -0.4)
b <- c(1, -0.1, 0.2, 0, 0.5, 0, 0.1)
shuffleProduct(a, b, depth = 2)

}
\seealso{
\code{\link[=tensorProduct]{tensorProduct()}}, \code{\link[=tensorInverse]{tensorInverse()}}, \code{\link[=signature]{signature()}}
}
