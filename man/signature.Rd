% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signature.R
\name{signature}
\alias{signature}
\title{Truncated path signature of a discrete path}
\usage{
signature(X, depth, includeLevelZero = TRUE, sep = ",")
}
\arguments{
\item{X}{a numeric matrix of shape \verb{T x d} (rows = ordered observations,
columns = channels). A numeric vector is treated as a 1D path
(\verb{T x 1}). Must be finite.}

\item{depth}{non-negative integer truncation level. \code{depth = 0} returns
only the level-0 term (\code{1}).}

\item{includeLevelZero}{if \code{TRUE} (default), the returned vector starts
with the empty-word entry (value \code{1}, name \code{""}). If \code{FALSE}, that
entry is dropped.}

\item{sep}{string separating letters in word names (default \code{","}).
For \code{d <= 9}, \code{sep = ""} gives compact \code{"11"}, \code{"12"}, ... names;
for \code{d >= 10} the default is required to stay unambiguous.}
}
\value{
a named numeric vector. Length is \code{sum(d^(0:depth))} if
\code{includeLevelZero = TRUE}, else \code{sum(d^(0:depth)) - 1}.
}
\description{
Computes the terminal value of the truncated path signature of \code{X} up to
level \code{depth}. Uses the exact piecewise-linear formulation (segment-wise
tensor exponentials and Chen products), so closed forms such as
\verb{(b - a)^k / k!} for linear paths are recovered to machine precision.
}
\section{Time augmentation}{

\code{signature()} does not augment with time. To include time as a channel,
pre-process with \code{\link[=timeAugment]{timeAugment()}}.
}

\section{Note on name clash}{

\code{PathSignatuR::signature()} masks \code{methods::signature()} on attach.
}

\examples{
# 1D path: signature term at level k equals (x_T - x_0)^k / k!
x <- cumsum(rnorm(50))
s <- signature(x, depth = 4)
expected <- (x[50] - x[1])^(0:4) / factorial(0:4)
all.equal(unname(s), expected)

}
\seealso{
\code{\link[=enumerateWords]{enumerateWords()}}, \code{\link[=signatureBatch]{signatureBatch()}}, \code{\link[=signaturePath]{signaturePath()}}
}
