% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logSignaturePathLyndon.R
\name{logSignaturePathLyndon}
\alias{logSignaturePathLyndon}
\title{Path-valued log-signature in the Lyndon basis}
\usage{
logSignaturePathLyndon(X, depth, sep = ",")
}
\arguments{
\item{X}{numeric matrix (T x d) or vector (treated as 1D path).}

\item{depth}{non-negative integer truncation level.}

\item{sep}{separator for Lyndon-word names (default \code{","}).}
}
\value{
Numeric matrix of shape \verb{T x q} where
\code{q = length(lyndonWords(d, depth))}. Column names are Lyndon
words joined by \code{sep}; row names are \code{NULL}. For \code{depth = 0}
returns a \verb{T x 0} matrix.
}
\description{
Returns the Lyndon-basis log-signature at every prefix of the path.
Row \code{t} is \code{logSignatureLyndon(X[1:t, , drop = FALSE], depth)};
row 1 is zero (log of identity), and row \code{T} matches
\code{logSignatureLyndon(X, depth)}.
}
\section{Implementation}{

Computes the tensor-basis running log-signature via
\code{logSignaturePath()} (one C++ call), then batches the projection
as a single \code{qr.coef} against the cached QR. The projection step
is one matrix back-solve regardless of \code{T}.
}

\examples{
X  <- matrix(cumsum(rnorm(60)), 30, 2)
bp <- logSignaturePathLyndon(X, depth = 3)
dim(bp)                          # 30 x 5
bp[1, ]                          # zero (log of identity)
all.equal(unname(bp[30, ]),
          unname(logSignatureLyndon(X, depth = 3)))

}
\seealso{
\code{\link[=logSignatureLyndon]{logSignatureLyndon()}}, \code{\link[=logSignaturePath]{logSignaturePath()}}
}
