% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensorInverse.R
\name{tensorInverse}
\alias{tensorInverse}
\title{Truncated tensor inverse}
\usage{
tensorInverse(a, depth)
}
\arguments{
\item{a}{numeric vector. Length must match \code{sum(d^(0:depth))} for
some positive integer \code{d}. Level-0 coefficient \code{a[1]} must equal 1
(within \code{1e-12}). Must be finite.}

\item{depth}{non-negative integer truncation level.}
}
\value{
a named numeric vector of the same length as \code{a}.
}
\description{
Computes \code{a^{-1}} in the truncated tensor algebra: the unique element
satisfying \verb{a (x) a^\{-1\} = e}, where \verb{e = (1, 0, 0, ...)} is the
identity tensor. Requires \code{a[1] = 1} (the standard condition under
which the inverse exists as a Neumann series).
}
\details{
The implementation uses the truncated Neumann expansion
\verb{a^\{-1\} = sum_\{k = 0\}^\{N\} (e - a)^\{(x) k\}},
which is finite at truncation level \code{N} because \code{(e - a)} has zero
level-0 coefficient and thus \verb{(e - a)^\{(x) k\}} has minimum level \code{k}.
}
\section{Signature reversal}{

If \code{X} is a path and \code{X_rev} is the same path traversed in reverse,
then \verb{signature(X) (x) signature(X_rev) = e}, so
\code{signature(X_rev) = tensorInverse(signature(X))}. This is the
standard way to invert signatures under path reversal.
}

\examples{
X <- matrix(cumsum(rnorm(40)), 20, 2)
s <- signature(X, depth = 3)
sInv <- tensorInverse(s, depth = 3)
# Verify: s (x) sInv ≈ e
tensorProduct(s, sInv, depth = 3)

}
\seealso{
\code{\link[=tensorProduct]{tensorProduct()}}, \code{\link[=signature]{signature()}}
}
