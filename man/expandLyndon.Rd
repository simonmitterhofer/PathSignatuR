% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expandLyndon.R
\name{expandLyndon}
\alias{expandLyndon}
\title{Expand Lyndon coordinates to the tensor basis}
\usage{
expandLyndon(beta, dim, depth, sep = ",")
}
\arguments{
\item{beta}{numeric vector of length \code{length(lyndonWords(dim, depth))}.}

\item{dim}{alphabet size, positive integer.}

\item{depth}{non-negative integer truncation level.}

\item{sep}{separator for tensor-word names (default \code{","}).}
}
\value{
Named numeric vector of length \code{sum(dim^(0:depth))} in
\code{enumerateWords} order. For \code{depth = 0}, returns \verb{c("" = 0)}.
}
\description{
Inverse of \code{\link[=logSignatureLyndon]{logSignatureLyndon()}}: given Lyndon coordinates \code{beta},
returns the corresponding free-Lie-algebra element in the tensor
basis as a named numeric vector of length \code{sum(dim^(0:depth))}.
}
\details{
Computationally one matrix-vector product against the cached
bracket-to-tensor matrix.
}
\examples{
X     <- matrix(cumsum(rnorm(60)), 30, 2)
beta  <- logSignatureLyndon(X, depth = 3)
lsT   <- expandLyndon(beta, dim = 2, depth = 3)
all.equal(unname(lsT), unname(logSignature(X, depth = 3)))

}
\seealso{
\code{\link[=logSignatureLyndon]{logSignatureLyndon()}}, \code{\link[=logSignature]{logSignature()}}
}
