% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logSignatureLyndon.R
\name{logSignatureLyndon}
\alias{logSignatureLyndon}
\title{Log-signature in the Lyndon basis}
\usage{
logSignatureLyndon(X, depth, sep = ",")
}
\arguments{
\item{X}{numeric matrix (T x d) or vector (treated as 1D path).}

\item{depth}{non-negative integer truncation level.}

\item{sep}{separator for Lyndon-word names (default \code{","}).}
}
\value{
Named numeric vector of length \code{length(lyndonWords(d, depth))}.
Names are Lyndon words joined by \code{sep}. For \code{depth = 0}, returns
\code{numeric(0)}.
}
\description{
Projects the log-signature onto the Lyndon basis of the free Lie
algebra, returning canonical independent coordinates. The Lyndon
basis has dimension given by Witt's formula
\verb{dim L_N(d) = sum_\{k=1\}^\{N\} (1/k) sum_\{j | k\} mu(j) d^(k/j)},
strictly smaller than the tensor-algebra dimension \code{sum(d^(0:N))}
for \verb{d >= 2, N >= 2}. For \verb{(d, N) = (3, 4)}: 32 vs 121.
}
\section{Why this exists}{

The log-signature lies in the free Lie algebra, a strict subspace
of the truncated tensor algebra. The tensor-basis representation
carries linear redundancy from the Jacobi identity and bracket
antisymmetry. Lyndon coordinates remove this algebraic
collinearity, yielding a canonical minimal representation —
orthogonal to and complementary with statistical reductions like
PCA or JL.
}

\section{Implementation}{

The \verb{(d, depth)} projection matrix \code{M} (tensor-basis expansions of
standard Lyndon brackets) and its QR decomposition are built once
per \verb{(d, depth)} and cached. Each call computes the tensor-basis
log-signature via the C++ kernel, then back-solves against the
cached QR.
}

\examples{
X    <- matrix(cumsum(rnorm(60)), 30, 2)
beta <- logSignatureLyndon(X, depth = 3)
length(beta)                   # 5 = length(lyndonWords(2, 3))
names(beta)                    # "1" "2" "1,2" "1,1,2" "1,2,2"

# Round-trip
all.equal(unname(expandLyndon(beta, 2, 3)),
          unname(logSignature(X, 3)))

}
\seealso{
\code{\link[=lyndonWords]{lyndonWords()}}, \code{\link[=logSignature]{logSignature()}}, \code{\link[=expandLyndon]{expandLyndon()}},
\code{\link[=logSignaturePathLyndon]{logSignaturePathLyndon()}}
}
