% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chenProduct.R
\name{chenProduct}
\alias{chenProduct}
\title{Chen product (alias for tensorProduct)}
\usage{
chenProduct(a, b, depth)
}
\arguments{
\item{a, b}{numeric vectors of equal length. Their length must equal
\code{sum(d^(0:depth))} for some positive integer \code{d}. Must be finite.}

\item{depth}{non-negative integer truncation level. Must match the
\code{depth} used to produce \code{a} and \code{b}.}
}
\value{
a named numeric vector of the same length as \code{a} and \code{b}.
}
\description{
The Chen product of two signatures is mathematically identical to the
truncated tensor product: for paths \code{X1}, \code{X2} sharing a join point,
\verb{signature(X1.X2) = signature(X1) (x) signature(X2)}. This function
is provided as a named alias of \code{\link[=tensorProduct]{tensorProduct()}} for code that
wants to emphasise the signature-concatenation semantics.
}
\details{
Use \code{chenProduct()} when combining signatures of adjacent path
segments; use \code{tensorProduct()} when working with general tensor
algebra elements. Both call the same underlying kernel.
}
\examples{
X <- matrix(cumsum(rnorm(60)), 30, 2)
X1 <- X[1:15, , drop = FALSE]
X2 <- X[15:30, , drop = FALSE]
s1 <- signature(X1, depth = 2)
s2 <- signature(X2, depth = 2)
sFull <- chenProduct(s1, s2, depth = 2)
all.equal(unname(sFull), unname(signature(X, depth = 2)))

}
\seealso{
\code{\link[=tensorProduct]{tensorProduct()}}, \code{\link[=signature]{signature()}}
}
