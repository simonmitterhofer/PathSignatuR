% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lyndonWords.R
\name{lyndonWords}
\alias{lyndonWords}
\title{Lyndon words over an alphabet of size d}
\usage{
lyndonWords(dim, depth)
}
\arguments{
\item{dim}{alphabet size, positive integer.}

\item{depth}{maximum word length, non-negative integer.}
}
\value{
A list of integer vectors, in canonical order: by length,
then lex within length. Empty list for \code{depth = 0}.
}
\description{
Enumerates all Lyndon words of length 1 to \code{depth} over the alphabet
\verb{\{1, ..., dim\}}. A Lyndon word is a non-empty word that is strictly
lexicographically less than all its proper rotations. Equivalently,
a Lyndon word is the unique lex-smallest representative of its
rotation class, restricted to aperiodic words.
}
\details{
Lyndon words of length up to \code{N} index a basis of the free Lie
algebra of rank \code{d} truncated at level \code{N}. The count at length \code{k}
is given by Witt's formula
\verb{L(d, k) = (1/k) sum_\{j | k\} mu(j) d^(k/j)},
which is strictly smaller than \code{d^k} for \verb{d >= 2, k >= 2}.
}
\examples{
lyndonWords(2, 3)
#> [[1]] 1
#> [[2]] 2
#> [[3]] 1 2
#> [[4]] 1 1 2
#> [[5]] 1 2 2

# Counts match Witt's formula
lengths(lyndonWords(3, 4))
length(lyndonWords(3, 4))    # 32

}
\seealso{
\code{\link[=enumerateWords]{enumerateWords()}}, \code{\link[=logSignatureLyndon]{logSignatureLyndon()}}
}
