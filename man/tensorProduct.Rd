% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensorProduct.R
\name{tensorProduct}
\alias{tensorProduct}
\title{Truncated tensor product}
\usage{
tensorProduct(a, b, depth)
}
\arguments{
\item{a, b}{numeric vectors of equal length. Their length must equal
\code{sum(d^(0:depth))} for some positive integer \code{d}. Must be finite.}

\item{depth}{non-negative integer truncation level. Must match the
\code{depth} used to produce \code{a} and \code{b}.}
}
\value{
a named numeric vector of the same length as \code{a} and \code{b},
in canonical word order.
}
\description{
Computes the truncated tensor product of two elements \code{a} and \code{b}
of the truncated tensor algebra. For each output word \code{w},
\verb{(a (x) b)[w] = sum over splits w = u.v of a[u] * b[v]}.
}
\details{
Both inputs must be numeric vectors of equal length matching
\code{sum(d^(0:depth))} for some integer \code{d}, which is inferred from
the length. The output sits in the same \code{enumerateWords} ordering
and carries canonical word names.
}
\section{Concatenation of signatures (Chen's identity)}{

If \code{X1} and \code{X2} are paths sharing a join point, then
\verb{signature(X1.X2) = signature(X1) (x) signature(X2)}. This makes
\code{tensorProduct()} the natural way to combine signatures of
adjacent path segments.
}

\examples{
a <- signature(matrix(cumsum(rnorm(20)), 10, 2), depth = 2)
b <- signature(matrix(cumsum(rnorm(20)), 10, 2), depth = 2)
tensorProduct(a, b, depth = 2)

}
\seealso{
\code{\link[=signature]{signature()}}, \code{\link[=enumerateWords]{enumerateWords()}}
}
