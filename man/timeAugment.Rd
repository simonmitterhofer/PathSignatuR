% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeAugment.R
\name{timeAugment}
\alias{timeAugment}
\title{Prepend a time channel to a path}
\usage{
timeAugment(X, time = NULL, scale = c("unit", "none"))
}
\arguments{
\item{X}{numeric matrix (T x d) or vector (treated as 1D path)}

\item{time}{optional numeric vector of length \code{T} giving the time
coordinate of each row. If \code{NULL}, defaults to \code{seq(0, 1, length.out = T)}
under \code{scale = "unit"}, or \verb{0, 1, ..., T-1} under \code{scale = "none"}.}

\item{scale}{one of \code{"unit"} (default, normalise to \verb{[0, 1]}) or
\code{"none"} (keep \code{time} as-is).}
}
\value{
a numeric matrix with \code{T} rows and \code{d + 1} columns. The first
column is named \code{"time"}.
}
\description{
Adds a time coordinate as the first column of the path. By default the
time grid is normalised to \verb{[0, 1]} (Remark 3.4 in Cuchiero & Möller),
which keeps signature values on a stable scale across rolling windows
of differing length. Supply a custom grid via \code{time}, or use
\code{scale = "none"} to keep elapsed numeric time.
}
\examples{
X <- matrix(rnorm(20), 10, 2)
Xt <- timeAugment(X)
colnames(Xt)
#> "time" "" ""

}
